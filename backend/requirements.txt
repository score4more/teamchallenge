# FastAPI and ASGI Server
fastapi==0.109.2
uvicorn==0.27.1

# Security and Authentication
python-multipart==0.0.9
python-jose[cryptography]==3.3.0
PyJWT==2.8.0

# Database and ORM
sqlalchemy==2.0.27

# Testing
pytest==7.4.4
pytest-asyncio==0.23.5
httpx==0.26.0
pytest-cov==4.1.0

# PDF Processing
pypdf==4.0.2  # Handles PDF splitting, text extraction, etc.

# Elasticsearch
elasticsearch==8.13.0  # Adjust the version based on your setup

# Async File Handling
aiofiles==23.2.1  # For handling file uploads asynchronously

# Task Queue (Optional)
celery==5.3.6  # If using Celery for background tasks
redis==5.0.1  # Required if using Celery with Redis as broker

# Production Deployment (Optional)
gunicorn==21.2.0  # If using Gunicorn in production
